interface ImportMetaEnv {
  readonly VITE_USE_MOCK?: string
  readonly VITE_API_URL?: string
  /** Grafana panel embed URL, shown in the history section once Lucas provides it. */
  readonly VITE_GRAFANA_URL?: string
}
